Roll No: 2401920310108
Week 1 - Day 1
Problem 2 - Remove Duplicates from Sorted Array

Example:
Input: nums = [0,0,1,1,1,2,2,3,3,4]
Output: 5

class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if not nums:
            return 0
        
        # Pointer for the place to insert unique value
        insert_pos = 1
        
        for i in range(1, len(nums)):
            if nums[i] != nums[i - 1]:
                nums[insert_pos] = nums[i]
                insert_pos += 1
        
        return insert_pos

Time Complexity: O(n)
Space Complexity: O(1)
